npm create vite@latest
npm i styled-components → the same semantic of ts but with css inside of code.
    npm i @types/styled-components -D 

---------------

ESLint 
    Definir padrão com diversas regras.


    ### Corrigindo erros definidos no padrao
        npx eslint src --ext .ts,.tsx --fix